import type { DrawerItem } from '@/src/core/tenant/types';

// Tambuzi has no quality screens in the legacy app yet. Drawer is intentionally empty
// until Tambuzi's feature set is defined; the user will still see the header + version.
export const tambuziDrawer: DrawerItem[] = [];
